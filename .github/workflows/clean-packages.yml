name: Clean packages
on:
  registry_package:
    types: []

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Run a one-line script
        run: echo ${{ toJSON(github.event.registry_package) }}
